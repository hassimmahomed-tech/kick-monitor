{
  "name": "kick-monitor",
  "version": "2.0.0",
  "description": "Kick casino streaming monitor - viewer polling + chat webhooks + analytics",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup-db": "node setup-db.js",
    "migrate": "node migrate-from-tasklet.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.12.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
